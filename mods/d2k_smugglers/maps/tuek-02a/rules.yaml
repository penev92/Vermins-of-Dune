^Palettes:
	IndexedPlayerPalette:
		PlayerIndex:
			Deserters: 239, 238, 237, 236, 235, 234, 233, 232, 231, 230, 229, 228, 227, 226, 225, 224

Player:
	PlayerResources:
		DefaultCash: 5000

World:
	LuaScript:
		Scripts: campaign-global.lua
	MapOptions:
		TechLevel: low
	ScriptLobbyDropdown@difficulty:
		ID: difficulty
		Label: Difficulty
		Values:
			easy: Easy
			normal: Normal
			hard: Hard
		Default: easy

carryall.reinforce:
	Cargo:
		MaxWeight: 10

upgrade.conyard:
	Buildable:
		Prerequisites: ~disabled

upgrade.barracks:
	Buildable:
		Prerequisites: ~disabled

upgrade.light:
	Buildable:
		Prerequisites: ~disabled

upgrade.heavy:
	Buildable:
		Prerequisites: ~disabled

concreteb:
	Buildable:
		Prerequisites: ~disabled

heavy_factory:
	Buildable:
		Prerequisites: ~disabled

medium_gun_turret:
	Buildable:
		Prerequisites: ~disabled

wall:
	Buildable:
		Prerequisites: ~disabled

outpost:
	Buildable:
		Prerequisites: barracks
